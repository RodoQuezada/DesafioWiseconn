#!/usr/bin/env node

'use strict';

process.title = 'bioxhome';
var path = require('path');
var fs = require('fs');
var server = path.join(path.dirname(fs.realpathSync(__filename)), '../server');
require(server + '/BHSCLI')();